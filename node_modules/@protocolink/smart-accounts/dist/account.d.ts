export declare function isSupportedSmartAccountId(chainId: number, id: string): boolean;
export declare function encodeSmartAccount(chainId: number, id: string, tos: string[], datas: string[], values: string[]): {
    to: string;
    data: string;
};
