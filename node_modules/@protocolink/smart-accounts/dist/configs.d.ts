export declare enum SmartAccountId {
    PORTUS = "portus"
}
export interface SmartAccount {
    id: string;
    executor: string;
}
export interface Config {
    chainId: number;
    smartAccounts: SmartAccount[];
}
export declare const configs: Config[];
export declare const supportedChainIds: number[], configMap: Record<number, Config>, smartAccountMap: Record<number, Record<string, SmartAccount>>, smartAccountIdMap: Record<number, string[]>;
export declare function getConfig(chainId: number): Config;
export declare function getSmartAccountIds(chainId: number): string[];
export declare function getSmartAccount(chainId: number, id: string): SmartAccount;
