import * as common from '@protocolink/common';
type ContractNames = 'Morpho' | 'MorphoFlashLoanCallback';
export interface MarketConfig {
    id: string;
    loanToken: common.Token;
    collateralToken: common.Token;
    oracle: string;
    irm: string;
    lltv: string;
}
export interface Config {
    chainId: number;
    contract: Record<ContractNames, string>;
    markets: MarketConfig[];
}
export declare const configs: Config[];
export declare const supportedChainIds: number[], configMap: Record<number, Config>, marketMap: Record<number, Record<string, MarketConfig>>;
export declare function getContractAddress(chainId: number, name: ContractNames): string;
export declare function getMarkets(chainId: number): MarketConfig[];
export declare function getMarket(chainId: number, id: string): MarketConfig;
export {};
