import * as common from '@protocolink/common';
import * as core from '@protocolink/core';
export type BorrowLogicTokenList = Record<string, common.Token[]>;
export type BorrowLogicFields = core.TokenOutFields<{
    marketId: string;
}>;
export type BorrowLogicOptions = Pick<core.GlobalOptions, 'account'>;
export declare class BorrowLogic extends core.Logic implements core.LogicTokenListInterface, core.LogicBuilderInterface {
    static id: string;
    static protocolId: string;
    static readonly supportedChainIds: number[];
    getTokenList(): Promise<BorrowLogicTokenList>;
    build(fields: BorrowLogicFields, options: BorrowLogicOptions): Promise<{
        to: string;
        data: string;
        inputs: core.DataType.InputStruct[];
        wrapMode: number;
        approveTo: string;
        callback: string;
    }>;
}
