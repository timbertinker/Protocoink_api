import * as common from '@protocolink/common';
import * as core from '@protocolink/core';
export interface CustomDataLogicFields {
    inputs?: common.TokenAmounts;
    outputs?: common.TokenAmounts;
    to: string;
    data: string;
}
export declare class CustomDataLogic extends core.Logic implements core.LogicBuilderInterface {
    static id: string;
    static protocolId: string;
    static readonly supportedChainIds: number[];
    build(fields: CustomDataLogicFields): Promise<{
        to: string;
        data: string;
        inputs: core.DataType.InputStruct[];
        wrapMode: number;
        approveTo: string;
        callback: string;
    }>;
}
