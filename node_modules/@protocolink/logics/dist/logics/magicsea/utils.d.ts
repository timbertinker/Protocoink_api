import { BigintIsh, Currency, Token, TokenAmount } from '@uniswap/sdk';
import * as common from '@protocolink/common';
export declare function toCurrency(token: common.TokenTypes): Currency;
export declare function toCurrencyAmount(token: common.TokenTypes, amount: BigintIsh): TokenAmount;
export declare function toUniToken(token: common.TokenTypes): Token;
export declare function toToken(token: Token): common.Token;
