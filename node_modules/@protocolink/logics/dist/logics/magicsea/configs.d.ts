import * as common from '@protocolink/common';
type ContractNames = 'Factory' | 'Router';
export interface Config {
    chainId: number;
    contract: Record<ContractNames, string>;
    tokenListUrls: string[];
    bases: common.Token[];
}
export declare const configs: Config[];
export declare const supportedChainIds: number[], configMap: Record<number, Config>;
export declare function getContractAddress(chainId: number, name: ContractNames): string;
export declare function getTokenListUrls(chainId: number): string[];
export declare function getBaseTokens(chainId: number): common.Token[];
export {};
