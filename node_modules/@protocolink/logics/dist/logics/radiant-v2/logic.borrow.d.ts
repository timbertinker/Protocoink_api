import * as aavev2 from '../../modules/aavev2';
import * as core from '@protocolink/core';
import { providers } from 'ethers';
export type BorrowLogicFields = aavev2.BorrowLogicFields;
export type BorrowLogicOptions = aavev2.BorrowLogicOptions;
export type BorrowLogicTokenList = aavev2.BorrowLogicTokenList;
export declare class BorrowLogic extends aavev2.BorrowLogic implements core.LogicTokenListInterface, core.LogicBuilderInterface {
    static protocolId: string;
    static readonly supportedChainIds: number[];
    constructor(chainId: number, provider?: providers.Provider);
}
