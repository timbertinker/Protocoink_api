import * as common from '@protocolink/common';
export type ContractNames = 'PoolMaster' | 'ClassicPoolFactory' | 'StablePoolFactory' | 'Router' | 'RouteHelper';
export interface Config {
    chainId: number;
    contract: Record<ContractNames, string>;
    baseTokenAddresses: string[];
    tokens: common.Token[];
}
export declare const configs: Config[];
export declare const supportedChainIds: number[], configMap: Record<number, Config>, tokenMap: Record<number, Record<string, common.Token>>;
export declare function getConfig(chainId: number): Config;
export declare function getContractAddress(chainId: number, name: ContractNames): string;
export declare function getToken(chainId: number, address: string): common.Token;
