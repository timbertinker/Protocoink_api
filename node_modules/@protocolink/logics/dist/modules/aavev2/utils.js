"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createWithdrawTokenList = exports.createDepositTokenList = void 0;
function createDepositTokenList(reserveTokens, tokenKey) {
    return reserveTokens.flatMap((reserveToken) => {
        const tokenList = [];
        if (reserveToken.asset.isWrapped) {
            tokenList.push([reserveToken.asset.unwrapped, reserveToken[tokenKey]]);
        }
        tokenList.push([reserveToken.asset, reserveToken[tokenKey]]);
        return tokenList;
    });
}
exports.createDepositTokenList = createDepositTokenList;
function createWithdrawTokenList(reserveTokens, tokenKey) {
    return reserveTokens.flatMap((reserveToken) => {
        const tokenList = [];
        if (reserveToken.asset.isWrapped) {
            tokenList.push([reserveToken[tokenKey], reserveToken.asset.unwrapped]);
        }
        tokenList.push([reserveToken[tokenKey], reserveToken.asset]);
        return tokenList;
    });
}
exports.createWithdrawTokenList = createWithdrawTokenList;
//# sourceMappingURL=utils.js.map